#include<stdio.h>
 #include<math.h>
 int main(){
     int i,n,count=0;
     scanf("%d",&n);
     for(i=1;i<=10;i++)
     {
         if(n%i==0)
         count=count+1;
     }
        if(count==2)
        {
            printf("Prime number");
        }
        else
        {
            printf("Not a Prime number");
        }
        return 0;
 }
