#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int n,m,sum=0;
    scanf("%d",&n);
    scanf("%d",&m);
    for(int i=n;i<=m;i++) 
    {
        sum=sum+i;
    }
        printf("%d",sum);
    return 0;
}
