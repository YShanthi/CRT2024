#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int n; 
    scanf("%d",&n);
    int rem=n%10;
    if(rem>=5)
    {
        printf("%d\n",n+(10-rem));
    }
    else
    {
        printf("%d\n",n-rem);
    }
    return 0;
}
